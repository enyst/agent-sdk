{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "partial-failures",
  "version": "1.0.0",
  "description": "One broken entry per component type, each isolated from its siblings.",
  "extensions": { "dev.openhands": {} }
}
