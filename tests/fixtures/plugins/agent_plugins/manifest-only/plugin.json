{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "manifest-only",
  "version": "1.0.0",
  "description": "A manifest and nothing else: every fixed location is absent."
}
