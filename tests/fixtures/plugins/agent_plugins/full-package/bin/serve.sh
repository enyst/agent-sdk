#!/usr/bin/env bash
# Never executed by the test suite: the fixture only needs a real file inside
# the package so the plugin-relative ``command`` resolves within the root.
echo "full-package stdio server"
